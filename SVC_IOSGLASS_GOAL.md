# svc-dashboard 移动端 iOS 26 "Liquid Glass" 风格重构 — 完整任务目标

## 一、设计语言参考（用户拍板：参考 iOS 文件 App 截图 + iOS 26 Liquid Glass）

用户给的参考图核心元素（必须逐条落实）：
1. **纯黑背景**（深色 #000，亮色主题对应纯白 #fff）+ 大量留白
2. **顶部不固定**：topbar 随内容滚动（sticky 都不要，滚动即离开视口），
   用"滚动后右下/顶部浮出圆形刷新按钮"保住刷新可达性（见 §三.4）
3. **topbar 极简**：左边服务器名（HOSTNAME，大标题粗体左对齐，
   iOS Large Title 风），右边一个圆形刷新图标按钮——没有别的
4. **底部操作栏改成悬浮胶囊**：不贴边、左右留 ~20px 边距、超大圆角(约 24-28px)、
   半透明模糊背景（backdrop-filter blur + saturate，深色 rgba(28,28,30,.72)、
   亮色 rgba(242,242,247,.72)）、细边框（rgba(255,255,255,.08)/rgba(0,0,0,.06)）、
   柔和阴影；底部离 safe-area 约 10-12px
5. **选中态**：页签选中用**内嵌深色胶囊**（iOS 26 风：选中 tab 背景是更深的
   胶囊块，图标+文字变强调蓝 #0A84FF），未选中白/灰
6. **搜索/筛选控件**：胶囊形（高度 ~44px，圆角=高一半，背景 #1C1C1E 深色/
   #fff 亮色，无边框）
7. **空状态**：灰色大图标 + 粗体主标题 + 灰色副标题，垂直居中，大量留白
8. **圆角体系**：小按钮 16-20px / 卡片 14-16px / 搜索胶囊 ~22px / 底部栏 26-28px，
   全站统一不混用
9. **字号层级**：Large Title 34px 粗体 / 空状态标题 22px 粗体 / 正文 15px /
   副文本 13px 灰 (#8E8E93)
10. **图标**：SF Symbols 风——线性 stroke、圆润线帽、无渐变无 emoji（现有 SVG 体系
    保持，补缺的用同风格）

## 二、改造范围（六页签全部）

### 1. 顶栏（滚动即走，不固定）
- 布局：`服务器名`(左, Large Title 28-34px 粗体) + 右侧圆形刷新按钮
  （点击刷新/长按锁定自动刷新——现有逻辑保留，锁定态=琥珀描边+锁角标）
- **去掉 position:sticky**：topbar 在文档流里，向下滚自然滚出视口
- 向上滚回顶部时 topbar 回来（自然滚动即可，无动画需求）
- header 背景改透明（无 blur 无边框——它不再悬浮），移除 border-bottom

### 2. 浮动刷新按钮（补偿 topbar 滚走）
- 当 topbar 滚出视口后，**右下角（底部栏上方 20px）浮出圆形玻璃刷新按钮**（48px，
  同底部栏玻璃材质）；topbar 可见时隐藏（IntersectionObserver 观察 header）
- 点击=刷新；长按=锁定/解锁；与 topbar 刷新同一状态

### 3. 底部页签栏 → 悬浮胶囊
- 容器：左右 margin 20px / 圆角 28px / 玻璃材质（§一.4 配方）/ padding 6px
- 6 个页签（概要/日志/Goal/服务/模型/ツール）等宽；选中=内嵌深胶囊+蓝，
  未选中=灰图标+灰字（10.5-11px）
- 页签图标 22px 上、文字下（现有 SVG 图标补齐每页一个）
- **主内容区 padding-bottom 加大**：底部栏高(~64px)+悬浮空隙(12px)+safe-area+16px
  余量；页面高度逻辑（track 跟随当前页）不得回归
- 手势（左右滑切页/边缘返回/行左滑）全部保留；页签点击目标 ≥44px

### 4. 卡片/列表/杂项统一质感
- statuscard/指标卡/goal 卡/服务行/日志行/ツール 各 section 卡：圆角 14-16px、
  背景 var(--bg-elev)、无边框或极淡边框、行距 10-12px
- chips（筛选/分类）：胶囊形（§一.6），选中=白底黑字（深色模式）/蓝底白字可选
- 表格（服务页桌面/移动卡片）：sticky thead 若因 topbar 不再固定而失去锚点，
  改为滚动容器内 sticky（top:0，相对滚动容器）
- 下拉刷新圆形指示器（已实现）位置微调：topbar 滚走后仍 fixed top 居中即可
- 空状态（无 goal/无日志/无文件等）：按 §一.7 重做（灰大图标+粗标题+灰副标题）
- toast/加载骨架：同玻璃材质/圆角统一

### 5. 明暗主题
- 全部新样式走 CSS 变量（--bg/--bg-elev/--text/--text-dim/--accent 等），
  亮色主题同步给 iOS 浅色系（#F2F2F7 背景/白卡片/#0A84FF 强调）
- meta theme-color 跟随

## 三、验收（必须逐条截图证据，用户明令"必须仔细验收"）

手机 390x844 + 桌面 1280x800 双视口，每条都要截图存 docs/ui-redesign/：

1. **顶栏**：顶部初始态（服务器名+刷新按钮，Large Title）截图；向下滚动 300px
   截图（topbar 已滚出视口，内容全屏）；向上滚回顶部截图（topbar 回来）
2. **浮动刷新按钮**：topbar 滚走后右下角玻璃圆钮出现（截图）；topbar 可见时不出现
   （IntersectionObserver 行为，截图对比）；点击刷新触发 load(true)（console log 证据）；
   长按锁定态（琥珀角标）截图
3. **底部悬浮胶囊栏**：初始态截图（玻璃质感/圆角/悬浮边距清晰）；6 页签逐个点击，
   选中内嵌胶囊+蓝色、未选中灰（每页一张）；内容不被底部栏遮挡（滚到每页底部截图，
   最后一条完整可见）
4. **页面高度不回归**：切页时 track 高度跟随当前页（对比矮页/高页两张）；
   双指缩放仍禁止（viewport 不变）
5. **手势回归**：左滑切页/右滑返回/行左滑按钮/下拉刷新圆形指示器（拖动/旋转/完成
   三阶段）各一张
6. **明暗两套**：系统深/浅切换各截一张概要页+工具页，无对比度崩坏（文字 ≥4.5:1）
7. **console 零报错**（双视口各一次全页遍历）
8. emoji 扫描 = 0；py_compile 通过；服务重启后全部复验一遍
9. 桌面 1280：布局不破（顶部/底部栏在桌面保持现有桌面形态或同样玻璃化均可，
   但不许出现手机元素错位——桌面端底部栏本来就是隐藏的，保持隐藏，仅移动端悬浮化）

## 四、边界

- 只动移动端样式层；桌面 ≥768 保持现有布局逻辑（可共享变量/材质升级但不重排）
- 不改任何后端 API；不改手势识别逻辑本体（只调容器/层级/边距）
- 玻璃材质在低端安卓降级：`@supports not (backdrop-filter:…)` → 不透明背景
- prefers-reduced-motion：浮动按钮显隐/过渡全部关动画
- i18n 三语全过（页签名/空状态文案用 L10N 键，不许硬编码中文）
