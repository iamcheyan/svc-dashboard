# svc-dashboard 移动端深度适配（手势+布局+体验）— 完整任务目标

## 一、背景

用户**主要在手机上**使用此面板（看 goal/负载/模型/日志，遥控服务器）。已有：深色主题、
卡片单列、下拉刷新（808db5d）、agent/goal 卡片、模型检测。3438 行单文件
dashboard.py（纯标准库+内嵌前端）。用户要求"好好想想"移动端还能做什么——
把它当成一个**手机 App** 来打磨，不是"能看的网页"。

## 二、手势系统（核心新增）

1. **滑动切页（核心）**：页面分区改为横向页签（概览 / Goal / 服务 / 模型 / 日志），
   左右滑动切换（带页面指示器，滑动跟手有阻尼、松手吸附）；顶栏可点击切页
2. **列表手势**：
   - goal 卡片左滑 → 露出「复制 resume 命令」按钮
   - 服务条目左滑 → 「复制端口/地址」
   - 日志条目长按 → 复制全文
3. **双击**：概览页双击负载卡 → 跳转 Goal 页；双击磁盘卡 → 展开 top 进程
4. **下拉刷新已有**——保持，但刷新完成加触觉反馈提示（navigator.vibrate(10)）
5. **捏合缩放图表**（若加图表）：负载/CPU 简单折线图（纯 canvas，最近 24 个采样
   存 localStorage）支持捏合调整时间窗——图表可选，手势不强依赖它
6. **边缘右滑返回**：非首页分区从左边缘右滑 = 返回概览（与切页手势区分：起点
   x<24px 才算边缘手势）

## 三、布局与可用性（手机优先）

1. **safe-area 适配**：viewport-fit=cover + env(safe-area-inset-*)（刘海屏/手势条不遮挡）
2. **底部页签栏**（拇指区）：概览/Goal/服务/模型 四个固定底栏图标（日志并入概览），
   与顶栏同步高亮；底栏半透明毛玻璃 backdrop-filter
3. **卡片信息密度**：手机上 goal 卡片默认收起次要信息（上下文体积/重试详情点开看），
   主要留 状态灯+名称+最近活动
4. **表格转卡片**：服务列表手机上已是列表——补每行右侧圆形「打开」按钮（直达
   http://host:port），44px 触控目标
5. **横屏适配**：≥768px 自动回桌面布局（现有 media query 保留）
6. **加载骨架屏**：切页时骨架占位（不要白屏闪）
7. **字号动态**：系统字体大小尊重（rem 基准，别硬 px 全部）

## 四、性能与电量（手机关切）

1. 自动刷新默认 30s（手机端），页面不可见（visibilitychange）暂停一切轮询
2. 触摸滚动 passive listener；动画只用 transform/opacity
3. iOS Safari 兼容检查（-webkit- 前缀、100dvh、momentum scroll）

## 五、验收标准

1. 手机 390x844：四页签左右滑动切换流畅（跟手+吸附），截图每页一张
2. goal 卡片左滑露按钮可点；长按日志可复制
3. 底部页签栏+safe-area 刘海屏模拟截图（CDP mobile emulation）无遮挡
4. 页面隐藏时轮询暂停（console 埋点证明 visibilitychange 生效）
5. 桌面 ≥1280 布局不回归（截图对比）
6. 下拉刷新+触觉反馈正常；边缘右滑返回概览
7. py_compile + 服务重启 curl 200 + 手机 URL 可访问
8. commit+push（中文信息）

## 六、边界

- 纯标准库单文件不变；不加框架
- 不做写操作类手势（杀进程等仍不进无鉴权面板）
- 桌面端不受影响（所有手势仅 touch 设备注册）
- 端口 80 不变；改前备份 dashboard.py.bak-<date>
